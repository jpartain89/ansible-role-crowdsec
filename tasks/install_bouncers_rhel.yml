- name: Crowdsec | install_bouncers_rhel | Install bouncer {{ cs_bouncer.key }}
  ansible.builtin.dnf:
    name: "{{ cs_bouncers_official[cs_bouncer.key].deb }}"
    state: present
    update_cache: true
