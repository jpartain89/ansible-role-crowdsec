[defaults]
# When the role is checked out inside a parent roles directory
# (e.g. roles/jpartain89.crowdsec), this lets ansible-lint and
# ansible-playbook resolve the 'jpartain89.crowdsec' role name.
roles_path = ..
